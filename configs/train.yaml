train:
  exp_name: "exp_001"
  device: "auto"            # "auto"|"cpu"|"cuda"
  seed: 42

  epochs: 30
  batch_size: 32
  lr: 0.0003
  weight_decay: 0.0001

  freeze_backbone_epochs: 2 # trains classifier head first
  early_stop_patience: 7

  log_every: 50
  save_every_epoch: true
